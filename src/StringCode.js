import React from 'react';
import { Table } from 'antd';
import 'antd/dist/antd.css';
const { stringCode } = require('./stringcode.js');
console.log(stringCode.code);

const StringCode = () => {
  return (
    // <Table columns={eval(stringCode.code)} dataSource={[{ name: 'jack' }]} />
    <></>
  );
};

export default StringCode;
